# Development Nginx for API routing
FROM nginx:alpine

# Note: nginx configuration is mounted as a volume in docker-compose.dev.yml
# No need to copy the config file during build

# Expose port
EXPOSE 80

# Start nginx
CMD ["nginx", "-g", "daemon off;"] 