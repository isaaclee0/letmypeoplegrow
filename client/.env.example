# WebSocket Configuration
# Controls how attendance updates are sent
# Values:
#   'true' = WebSocket only (no REST API fallback)
#   'fallback' = WebSocket with REST API fallback (default)
#   'false' = REST API only (WebSocket disabled)
VITE_USE_WEBSOCKETS=fallback

# Other environment variables...
# Add your other environment variables here
